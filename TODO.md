# Before this site goes live

The following are placeholders in the current build and should be replaced
with real details before publishing:

- [ ] **WhatsApp number** — currently a placeholder (`https://wa.me/27000000000`)
      in `index.html` (nav, hero, package, contact section, floating button)
      and `assets/js/main.js` is not affected. Search for `27000000000`.
- [ ] **Email address** — currently `info@barsgraphics.co.za`, a guessed
      address based on a domain that may not exist yet. Confirm the real
      inbox and domain to use.
- [ ] **Package inclusions** — the R6,000 package description is written
      generally (no specific hours, photo counts, video length or turnaround
      time are promised) since those weren't provided. Add specifics if you
      want them stated on the site.
- [ ] **Portfolio / gallery** — the "Our Work" section currently shows
      color-coded placeholder tiles instead of real photos, since no event
      photos were supplied yet. Swap in real images once available.
- [ ] **Social links** — no Instagram/Facebook/TikTok links are included.
      Add them to the footer and contact section if Bars Graphics has active
      accounts.
- [ ] **Domain** — the site currently has no custom domain configured. Once
      GitHub Pages is enabled, a domain (e.g. via domains.co.za, per Blue
      Monday Studios' standard) can be pointed at it.
